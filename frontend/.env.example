# ---- Frontend (Vercel) ----

# The base URL of the backend API on Railway. No trailing slash.
# Vite exposes only vars prefixed with VITE_ to the browser, which is correct
# here — this is a public URL, not a secret.
VITE_API_BASE=https://petty-judge-production.up.railway.app
